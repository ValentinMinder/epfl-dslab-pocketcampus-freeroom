<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	package="org.pocketcampus" android:versionCode="1" android:versionName="1.0">


	<application android:icon="@drawable/icon" android:label="@string/app_name">
		
		<!-- Main Screen -->
		<activity android:name=".plugin.mainscreen.MainscreenDisplay">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>
		<activity android:name=".plugin.mainscreen.MainscreenConfiguration"></activity>
		
		<!-- News Plugin -->
		<activity android:name=".plugin.news.NewsConfiguration"></activity>
		<activity android:name=".plugin.news.NewsDisplay"
                  android:configChanges="keyboardHidden|orientation">
		</activity>
        
        <!-- Food Plugin -->
        <activity android:name=".plugin.menu.MenuConfiguration"></activity>
        <activity android:name=".plugin.menu.MenuDisplay"></activity>
        
	</application>
		
	<uses-permission android:name="android.permission.INTERNET" />
</manifest>