package org.pocketcampus.utils;

import android.content.Context;
import android.widget.LinearLayout;

public class PluginTabIndicator extends LinearLayout {
	public PluginTabIndicator(Context context, String label) {
		super(context);
		/*
		View tab = View.inflate(context, R.layout.feature_tab_indicator,
				this);

		TextView tv = (TextView) tab.findViewById(R.id.feature_tab_label);
		tv.setText(label);*/
	}
}
